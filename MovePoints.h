#pragma once

#include <Eigen/Dense>
#include <iostream>
#include <vector>

using namespace std;
using namespace Eigen;

// Now this is moving the points back the other way, but how do we update the forces
void movePoints(const MatrixXd &V, const MatrixXi &F, const VectorXd &forces, const VectorXd &weights) {

}
